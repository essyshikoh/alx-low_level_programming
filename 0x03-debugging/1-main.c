#include <tdio.h>

/**
 * main - causes an infinit loop
 *
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;

	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf(Infinite loop avoided !\\O / \n");
	return (0);
}
